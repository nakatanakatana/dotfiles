layout_git_main_root() {
  if git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    export MAIN_REPO_ROOT=$(cd "$(git rev-parse --git-common-dir)/.." && pwd)
    echo "➔ Git main repository: $MAIN_REPO_ROOT"
  fi
}
