{
  "colorScheme": "solarized dark",
  "enableTerminalSandbox": true,
  "model": "Gemini 3.5 Flash (High)",
  "notifications": true,
  "permissions": {
    "allow": [
      "command(buf)",
      "command(cat)",
      "command(gh api --method GET)",
      "command(gh auth status)",
      "command(gh pr list)",
      "command(gh pr view)",
      "command(gh run list)",
      "command(gh run view)",
      "command(gh search prs)",
      "command(git add)",
      "command(git branch)",
      "command(git diff)",
      "command(git log)",
      "command(git notes)",
      "command(git remote -v)",
      "command(git show)",
      "command(git status)",
      "command(glab auth status)",
      "command(glab ci status)",
      "command(glab ci trace)",
      "command(glab ci view)",
      "command(glab issue list)",
      "command(glab issue view)",
      "command(glab mr list)",
      "command(glab mr view)",
      "command(glab run list)",
      "command(glab run view)",
      "command(go build)",
      "command(go test)",
      "command(go tool)",
      "command(golangci-lint)",
      "command(grep)",
      "command(head)",
      "command(ls)",
      "command(make)",
      "command(mkdir)",
      "command(npm ci)",
      "command(npm run)",
      "command(npm test)",
      "command(CI=true npm test)",
      "command(openspec)",
      "command(rm -rf conductor/tracks/)",
      "command(sqlc)",
      "command(tail)",
      "command(wc)"
    ]
  },
  "showFeedbackSurvey": false,
  "statusLine": {
    "type": "command",
    "command": "node ~/.gemini/antigravity-cli/statusline.ts",
    "enabled": true
  },
  "title": {
    "type": "command",
    "command": "node ~/.gemini/antigravity-cli/title.ts"
  },
  "trustedWorkspaces": [
    "/home/tanaka/repos/gitlab.com/eseikatsu/sandbox/yu-nakamura/dotfiles",
    "/home/tanaka/.wt/feed-reader/ex-tanstackdb",
    "/home/tanaka/.wt/feed-reader/improve-test"
  ]
}
